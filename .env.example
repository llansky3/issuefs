# JIRA API Configuration (REQUIRED)
JIRA_URL=https://jira.example.com
JIRA_API_TOKEN=your_jira_api_token_here

# Mount Point (OPTIONAL)
# Default location where the filesystem will be mounted
# 
# If not set, defaults to: /tmp/jira
# 
# Examples:
# - Mount in project directory:
#   MOUNTPOINT=.issuefs/mnt
#
# - Use absolute path:
#   MOUNTPOINT=/mnt/jira
#
# MOUNTPOINT=.issuefs/mnt

# Persistent Configuration Storage (OPTIONAL)
# Where to store persistent query configurations
# 
# If not set, defaults to: ~/.issuefs/persistent.yaml
# 
# Examples:
# - Store with project (can be committed to git):
#   PERSISTENT_CONFIG=./jira_queries.yaml
#
# - Use a shared team configuration:
#   PERSISTENT_CONFIG=/shared/team/jira_queries.yaml
#
# - Use absolute path:
#   PERSISTENT_CONFIG=/home/user/my_queries.yaml
#
# PERSISTENT_CONFIG=./jira_queries.yaml
